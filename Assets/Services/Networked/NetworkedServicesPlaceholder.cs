// Act II (Phase 10+): implement IProgressionService, IInventoryService, IQuestService here.
// Each implementation sends server RPCs instead of writing to the local database.
// GameServices.Initialize(db, characterId, networked: true) will instantiate these instead of Local/.
namespace Kaligo.Services.Networked { }
